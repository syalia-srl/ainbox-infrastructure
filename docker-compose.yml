version: "3.8"

# Plantilla base (YAML Anchor)
x-superbot-base: &base
  image: ${IMAGE_TAG:-superbot:latest}
  restart: unless-stopped
  # ESTA ES LA MAGIA: El contenedor usa la red de tu PC directamente.
  # Lo que levante el entrypoint según el JSON, se expone instantáneamente.
  network_mode: "host"

services:
  # --------------------------------------------------------
  # PROFILE: GPU
  # --------------------------------------------------------
  superbot-gpu:
    <<: *base
    container_name: superbot_engine_gpu
    profiles: ["gpu"]
    volumes:
      - ./${CONFIG_PATH}:/app/config/superbot_config.json:ro
      # - ./build/entrypoint.sh:/app/entrypoint.sh:ro  # <-- HACK LOCAL INTACTO
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # --------------------------------------------------------
  # PROFILE: CPU
  # --------------------------------------------------------
  superbot-cpu:
    <<: *base
    container_name: superbot_engine_cpu
    profiles: ["cpu"]
    environment:
      - LD_LIBRARY_PATH=/usr/lib:/usr/local/cuda/lib64/stubs
    volumes:
      - ./${CONFIG_PATH}:/app/config/superbot_config.json:ro
      # - ./build/entrypoint.sh:/app/entrypoint.sh:ro  # <-- HACK LOCAL INTACTO